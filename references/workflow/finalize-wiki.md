# finalize-wiki

Command:

```bash
deepwiki finalize <project_path>
```

Checks that planned pages exist, menu/doc-map generation succeeds, Mermaid blocks are repaired and optionally validated, output quality passes, and local Markdown links resolve.

Failure: stop and report the failed page or link.
