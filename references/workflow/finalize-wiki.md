# finalize-wiki

Command:

```bash
deepwiki finalize <project_path>
```

Checks that planned pages exist, local Markdown links resolve, and menu/doc-map generation succeeds.

Failure: stop and report the failed page or link.
