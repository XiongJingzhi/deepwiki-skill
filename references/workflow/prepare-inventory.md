# prepare-inventory

Command:

```bash
deepwiki prepare-inventory <project_path>
```

Output: `.deepwiki/cache/project-inventory.json`.

Collect file paths, categories, language hints, config files, docs, entry candidates, and basic stats. Do not parse ASTs or build module analysis.
