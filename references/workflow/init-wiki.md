# init-wiki

Command:

```bash
deepwiki init <project_path>
```

Creates `.deepwiki/cache/page-context/`, `.deepwiki/state/`, `.deepwiki/wiki/`, and `config.yaml`.

Failure: stop if the directory cannot be created.
