{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://deepwiki.sh/schemas/module-analysis-schema.json",
  "title": "Module Analysis Cache",
  "description": "Schema for AI-generated per-module analysis output (extract-docs). Written incrementally after each module is analyzed; consumed by synthesize-deps and generate-module-docs.",
  "type": "object",
  "required": ["cache_schema_version", "generated_at", "modules"],
  "properties": {
    "cache_schema_version": {
      "type": "integer",
      "description": "Schema version, matches CACHE_SCHEMA_VERSION in common.py"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of last write"
    },
    "modules": {
      "type": "object",
      "description": "Map of module-name -> ModuleAnalysis. Keys match the module names in structure.json.",
      "additionalProperties": {
        "$ref": "#/definitions/ModuleAnalysis"
      }
    }
  },
  "definitions": {
    "ModuleAnalysis": {
      "type": "object",
      "required": [
        "module_path",
        "code_purpose",
        "analysis_depth",
        "files",
        "module_summary",
        "module_role",
        "upstream_inputs",
        "downstream_outputs",
        "risk_points",
        "extension_points",
        "selected_components",
        "dependency_hints",
        "semantic_group"
      ],
      "properties": {
        "module_path": {
          "type": "string",
          "description": "Relative path of the module root (e.g. 'src/auth')"
        },
        "code_purpose": {
          "type": "string",
          "enum": ["Entry", "Agent", "Page", "Widget", "Service", "Api", "Dao", "Model", "Config", "Database", "Util", "Command", "Other"],
          "description": "CodePurpose determined for this module (dominant purpose across files)"
        },
        "analysis_depth": {
          "type": "string",
          "enum": ["deep", "standard", "quick"],
          "description": "Highest analysis depth applied to any file in this module"
        },
        "module_summary": {
          "type": "string",
          "description": "1-2 sentence summary of module responsibility and architecture role"
        },
        "module_role": {
          "type": "string",
          "description": "Reader-facing role of this module in the system model"
        },
        "upstream_inputs": {
          "type": "array",
          "description": "External modules, runtime events, files, requests, or data sources that feed this module",
          "items": { "type": "string" }
        },
        "downstream_outputs": {
          "type": "array",
          "description": "Outputs, side effects, calls, documents, or state changes produced by this module",
          "items": { "type": "string" }
        },
        "risk_points": {
          "type": "array",
          "description": "Known risks, fragile assumptions, or boundary conditions relevant to maintenance",
          "items": { "type": "string" }
        },
        "extension_points": {
          "type": "array",
          "description": "Stable places where future behavior can be extended or changed",
          "items": { "type": "string" }
        },
        "selected_components": {
          "type": "array",
          "description": "Document components selected for this module (from components-registry.yaml / components-guide.md). generate-module-docs uses this directly without re-deciding.",
          "items": { "type": "string" },
          "examples": [["overview", "api-table", "sequence-diagram", "code-walkthrough", "nav-links"]]
        },
        "dependency_hints": {
          "type": "object",
          "description": "Lightweight dependency summary extracted during extract-docs, used as synthesize-deps base input",
          "properties": {
            "imports": {
              "type": "array",
              "description": "Module names this module imports/depends on",
              "items": { "type": "string" }
            },
            "imported_by": {
              "type": "array",
              "description": "Module names that import this module (reverse deps)",
              "items": { "type": "string" }
            }
          },
          "additionalProperties": false
        },
        "semantic_group": {
          "type": "string",
          "description": "AI free-text annotation of this module's semantic theme, used as soft signal for menu grouping (e.g. '认证与鉴权', '消息路由', 'Data Persistence'). Named after reader-facing concepts, NOT CodePurpose enums."
        },
        "semantic_group_confidence": {
          "type": "string",
          "enum": ["high", "low"],
          "description": "high = module has a clear, distinct semantic boundary; low = AI is uncertain (overlapping responsibilities or unclear scope). generate-menu reduces grouping weight for low-confidence entries.",
          "default": "high"
        },
        "semantic_group_override": {
          "type": "boolean",
          "description": "true means this semantic_group comes from deep source analysis and differs from generate-skeleton suggestion. generate-menu will prioritize this naming and write back the correction to the skeleton file.",
          "default": false
        },
        "files": {
          "type": "array",
          "description": "Per-file analysis results for files in this module",
          "items": { "$ref": "#/definitions/FileAnalysis" }
        }
      },
      "additionalProperties": false
    },
    "FileAnalysis": {
      "type": "object",
      "required": ["path", "code_purpose", "complexity_score", "summary", "public_interfaces", "key_insights", "confidence"],
      "properties": {
        "path": {
          "type": "string",
          "description": "Relative file path from project root"
        },
        "code_purpose": {
          "type": "string",
          "enum": ["Entry", "Agent", "Page", "Widget", "Service", "Api", "Dao", "Model", "Config", "Database", "Util", "Command", "Test", "Other"],
          "description": "CodePurpose for this specific file"
        },
        "complexity_score": {
          "type": "number",
          "description": "Complexity score from structure.json (used for analysis depth selection)"
        },
        "summary": {
          "type": "string",
          "description": "1-2 sentence description of what this file does and why it exists"
        },
        "public_interfaces": {
          "type": "array",
          "description": "Exported functions, classes, and types",
          "items": { "$ref": "#/definitions/PublicInterface" }
        },
        "core_source_ranges": {
          "type": "array",
          "description": "Important source ranges suitable for Relevant source files and core logic walkthroughs",
          "items": { "$ref": "#/definitions/SourceRange" }
        },
        "internal_dependencies": {
          "type": "array",
          "description": "Intra-module dependencies (other files in same module this file imports)",
          "items": { "type": "string" }
        },
        "design_patterns": {
          "type": "array",
          "description": "Detected design patterns (e.g. singleton, strategy, observer)",
          "items": { "type": "string" }
        },
        "key_insights": {
          "type": "array",
          "description": "2-5 bullet points explaining WHY the code is designed this way (design intent, trade-offs)",
          "items": { "type": "string" },
          "maxItems": 5
        },
        "confidence": {
          "type": "string",
          "enum": ["high", "medium", "low"],
          "description": "Confidence level of the analysis. high=direct source evidence, medium=inferred from structure, low=inferred from naming"
        }
      },
      "additionalProperties": false
    },
    "PublicInterface": {
      "type": "object",
      "required": ["name", "type", "line", "description"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Function, class, or type name"
        },
        "type": {
          "type": "string",
          "enum": ["function", "class", "interface", "type", "enum", "constant", "method"],
          "description": "Kind of exported symbol"
        },
        "signature": {
          "type": "string",
          "description": "Full signature string (e.g. 'def login(username: str, password: str) -> Token')"
        },
        "line": {
          "type": "integer",
          "description": "Line number in source file"
        },
        "end_line": {
          "type": "integer",
          "description": "Inclusive end line in source file when known"
        },
        "description": {
          "type": "string",
          "description": "What this interface does and when to use it (min 1 sentence)"
        },
        "params": {
          "type": "array",
          "description": "Parameter descriptions (optional, for complex functions)",
          "items": {
            "type": "object",
            "required": ["name", "type", "description"],
            "properties": {
              "name": { "type": "string" },
              "type": { "type": "string" },
              "description": { "type": "string" },
              "required": { "type": "boolean", "default": true }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    },
    "SourceRange": {
      "type": "object",
      "required": ["label", "start_line", "end_line", "reason"],
      "properties": {
        "label": {
          "type": "string",
          "description": "Short label for this source range"
        },
        "start_line": {
          "type": "integer",
          "description": "1-based inclusive start line"
        },
        "end_line": {
          "type": "integer",
          "description": "1-based inclusive end line"
        },
        "reason": {
          "type": "string",
          "description": "Why this range matters for the generated document"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
